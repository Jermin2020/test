<?xml version="1.0" encoding="UTF-8"?>

<project name="ant-jmeter-test" default="all" basedir=".">
<tstamp>
<format property="time" pattern="MM-dd-hh-mm" />
</tstamp>
<!-- 需要改成自己本地的 Jmeter 目录--> 
<property name="jmeter.home" value="/test/apache-jmeter-3.3" />
<!-- jmeter生成jtl格式的结果报告的路径--> 
<property name="jmeter.result.jtl.dir" value="/test/log/jtl" />
<!-- jmeter生成html格式的结果报告的路径-->
<property name="jmeter.result.html.dir" value="/test/log/html" />
<property name="jmeter.result.html.dir2" value="/test/log/htmlx" />
<!-- 生成的报告的前缀--> 
<property name="ReportName" value="TestReport" />
<property name="jmeter.result.jtlName" value="${jmeter.result.jtl.dir}/${BUILD_NUMBER}.jtl" />
<property name="jmeter.result.htmlName" value="${jmeter.result.html.dir}/${BUILD_NUMBER}.html" />
<property name="jmeter.result.htmlName2" value="${jmeter.result.html.dir2}/${BUILD_NUMBER}.html" />



<!-- 接收测试报告的邮箱-->
<!-- <property name="mail_to" value="709079155@qq.com" /> -->
<!-- 电脑地址 -->
<!-- <property name="ComputerName" value="gxx" /> -->
<target name="all">
<antcall target="test" />
<antcall target="report" />
</target>

<target name="test">
<taskdef name="jmeter" classname="org.programmerplanet.ant.taskdefs.jmeter.JMeterTask" />
<jmeter jmeterhome="${jmeter.home}" resultlog="${jmeter.result.jtl.dir}/${BUILD_NUMBER}.jtl">
<!-- 声明要运行的脚本。"*.jmx"指包含此目录下的所有jmeter脚本-->
<testplans dir="/test/apache-jmeter-3.3/bin" includes="price.jmx" />
<property name="jmeter.save.saveservice.output_format" value="xml"/>

</jmeter>
</target>

<target name="report">
<tstamp> <format property="report.datestamp" pattern="yyyy/MM/dd HH:mm" /></tstamp>
<!-- 
in：生成的.jtl文件的路径
out：生成的.html文件的路径
style：从.jtl转换成.html所使用的样式，这些文件也在extras目录下，这个是最重要的部分，如果我们想定制报告可以自行定义这个文件或者从网上找一些合适的文件。-->
<xslt 
classpathref="xslt.classpath"
force="true"
in="${jmeter.result.jtlName}"
out="${jmeter.result.htmlName}"
style="${jmeter.home}/extras/jmeter-results-report_21.xsl">
<param name="dateReport" expression="${report.datestamp}"/>
</xslt>

<xslt 
classpathref="xslt.classpath"
force="true"
in="${jmeter.result.jtlName}"
out="${jmeter.result.htmlName2}"
style="${jmeter.home}/extras/jmeter.results.shanhe.me.xsl">
<param name="dateReport" expression="${report.datestamp}"/>
</xslt>

<!-- 因为上面生成报告的时候，不会将相关的图片也一起拷贝至目标目录，所以，需要手动拷贝 --> 
<copy todir="${jmeter.result.html.dir}">
<fileset dir="${jmeter.home}/extras">
<include name="collapse.png" />
<include name="expand.png" />
</fileset>
</copy>
<copy todir="${jmeter.result.html.dir2}">
<fileset dir="${jmeter.home}/extras">
<include name="collapse.png" />
<include name="expand.png" />
</fileset>
</copy>
</target>

<path id="xslt.classpath">
<fileset dir="${jmeter.home}/lib" includes="xalan*.jar"/>
<fileset dir="${jmeter.home}/lib" includes="serializer*.jar"/>
</path>
</project>